{
  "name": "wacker-examples-mono-repo",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "start": "npm run build && lerna run start --parallel",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.13.1",
    "rimraf": "^2.6.3",
    "wacker": "file:../.."
  }
}
